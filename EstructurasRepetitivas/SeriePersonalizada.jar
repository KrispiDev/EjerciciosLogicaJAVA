/**
** Explicación paso a paso:
** Importar la clase Scanner: Importamos java.util.Scanner para utilizar la clase Scanner y leer la entrada del usuario.
** Crear un objeto Scanner: Creamos un objeto Scanner llamado scanner para leer la cantidad de términos desde la consola.
** Solicitar al usuario la cantidad de términos: Se solicita al usuario que ingrese la cantidad de términos para la serie.
** Validar la cantidad de términos: Se verifica que la cantidad de términos sea mayor que cero. Si no lo es, se imprime un mensaje de error y se termina el programa.
** Calcular y mostrar la serie personalizada: Si la cantidad de términos es válida, se llama a la función calcularSeriePersonalizada para calcular y mostrar la serie 
** con la cantidad de términos especificada.
** Función calcularSeriePersonalizada: Dentro de esta función, se utiliza un bucle for para calcular la sumatoria de los términos de la serie. Para cada término, se 
** calcula el numerador y el denominador según la progresión aritmética y geométrica dadas.
** Imprimir el resultado: Se imprime en la consola el resultado de la serie con la cantidad de términos especificada.
**/

import java.util.Scanner;

public class SeriePersonalizada {

    public static void main(String[] args) {
        // Crear un objeto Scanner para leer la entrada del usuario
        Scanner scanner = new Scanner(System.in);

        // Solicitar al usuario el número de términos para la serie
        System.out.print("Ingrese la cantidad de términos para la serie: ");
        int numTerminos = scanner.nextInt();

        // Validar que el número de términos sea mayor que cero
        if (numTerminos <= 0) {
            System.out.println("La cantidad de términos debe ser mayor que cero. Programa finalizado.");
        } else {
            // Calcular y mostrar la serie personalizada
            double resultado = calcularSeriePersonalizada(numTerminos);
            System.out.println("El resultado de la serie con " + numTerminos + " términos es: " + resultado);
        }
    }

    // Función para calcular la serie personalizada con un número de términos específico
    static double calcularSeriePersonalizada(int numTerminos) {
        double suma = 0.0;

        // Calcular la sumatoria de los términos de la serie
        for (int n = 0; n < numTerminos; n++) {
            // Calcular el numerador y el denominador de cada término
            int numerador = n * 5;
            int denominador = (int) Math.pow(3, n);

            // Calcular el término actual y sumarlo a la suma total
            suma += (double) numerador / denominador;
        }

        return suma;
    }
}
